// a counter class to make sure we have the same counter across all classes (Wrapper class or something idk i failed oop)
public class Counter {
    public int value;
    public Counter(int value) { this.value = value; }
    
}
